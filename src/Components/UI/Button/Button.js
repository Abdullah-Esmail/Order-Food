// import React from "react";
// import style "./Button.module.css";

// const Button = (props) => {
//   return <button className={style["global-button"]} type={props.type || "button"}>{props.children}</button>;
// };

// // export default Button;
